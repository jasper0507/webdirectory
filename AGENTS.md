## 文档语言

除固定格式、代码标识、命令及其他确有必要的内容外，项目文档默认使用简体中文。

## Agent skills

### Issue tracker

Issue 与规格通过 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分诊标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

这是单上下文仓库。详见 `docs/agents/domain.md`。
