# 个人书签门户

本上下文描述一个公开门户：它呈现由唯一维护者管理的书签目录，便于快速查找个人收藏的网站。

## Language

**维护者（Maintainer）**:
唯一能够修改书签源并发布门户的人。
_Avoid_: 用户、管理员、贡献者

**访客（Visitor）**:
能够浏览或搜索已发布门户，但不能修改书签的人。
_Avoid_: 用户、成员

**书签目录（Bookmark Catalog）**:
根据书签源完整呈现的公开书签集合。
_Avoid_: 书签镜像、浏览器书签

**书签源（Bookmark Source）**:
唯一且由维护者手工维护的 JSON 配置文件，权威定义书签目录。
_Avoid_: 浏览器导出、数据库、生成数据、YAML

**迁移文件（Migration File）**:
仅用于首次生成书签源的浏览器书签 HTML 导出文件；迁移只读取标题与 URL，分类统一设为 `未分类`，并在公开页面信息足够可靠时生成不超过 15 个字符的简体中文描述。完成迁移后，它不作为运行时输入或持续维护来源。
_Avoid_: 书签源、导入功能

**书签条目（Bookmark Entry）**:
由标题和 URL 定义的公开网站引用，可以附带一个分类和描述；分类不是条目成立的条件。书签目录内不允许出现重复标题或重复的标准化 URL。标题去除首尾空格并做 Unicode 标准化后按大小写敏感方式比较。
_Avoid_: 站点、链接、资源

**分类（Category）**:
书签条目可选携带的单个分组名称；空值不参与汇总与筛选。系统从所有非空分类值自动汇总分类，不单独维护分类记录。分类名去除首尾空格并做 Unicode 标准化后仍按大小写敏感方式区分。
_Avoid_: 文件夹、标签

**添加面板（Add Panel）**:
帮助维护者校验新条目并下载更新后书签源的公开页面工具；任何访客都可以打开它，但它不用于身份验证，也不直接写入仓库或发布站点。
_Avoid_: 管理后台、在线编辑器
