# 个人书签门户

本上下文描述一个公开门户：它呈现由唯一维护者管理的书签目录，便于快速查找个人收藏的网站。

## Language

**维护者（Maintainer）**:
唯一能够修改门户源并发布门户的人。每个公开部署只有一名维护者；fork 后的仓库由该 fork 的维护者修改自己的门户源。
_Avoid_: 用户、管理员、贡献者

**访客（Visitor）**:
能够浏览或搜索已发布门户，但不能让权威门户源发生变更的人。
_Avoid_: 用户、成员

**门户源（Portal Source）**:
唯一且由维护者维护的 JSON 配置，权威定义站点身份与书签目录。后续改写的厅面选项也放在这份配置里。公开厅面不提供写入入口；维护者可以直接编辑这份文件，也可以通过收录扩展产生对该文件的变更。
_Avoid_: 书签源、数据库、生成数据、YAML、多文件配置、公开页添加面板、在线编辑

**站点身份（Site Identity）**:
门户源中由维护者填写的厅面槽位，包括字标、碑题、眉题、汇流短句、耳语、页脚印戳等可改文案；它不包含书签条目，也不包含七词。
_Avoid_: 主题、品牌系统、皮肤、前端文案

**七词（Seven Words）**:
搜索厅展示的快捷标签：按书签目录中各标签被条目引用的次数降序排列，最多七个；不足七个则有几个显示几个。次数相同则按该标签首次出现的顺序。
_Avoid_: 固定文案、分类、热门

**书签目录（Bookmark Catalog）**:
根据门户源完整呈现的公开书签集合。
_Avoid_: 书签镜像、浏览器书签

**迁移文件（Migration File）**:
仅用于首次生成门户源条目的浏览器书签 HTML 导出文件；迁移只读取标题与 URL，并把原文件夹名写成标签；若没有任何标签，则写入标签 `未分类`。公开页面信息足够可靠时生成不超过 15 个字符的简体中文描述。完成迁移后，它不作为运行时输入或持续维护来源。
_Avoid_: 门户源、导入功能、收录扩展

**收录扩展（Capture Extension）**:
与门户仓库分开维护的配套浏览器扩展，产品名为门户收录。维护者（含 fork 后的维护者）在选项中指定自己的 GitHub 仓库，打开侧栏把当前页预填成条目草稿，可再排队数条；确认后只把新书签条目写入该仓库的门户源，不改站点身份。标题或标准化 URL 与目录中已有条目重复则拒绝写入。条目草稿保存在浏览器本地，侧栏关掉也不丢。它不是公开厅面上的工具，不是迁移文件，也不把浏览器书签树当作权威源。
_Avoid_: 导入扩展、添加面板、书签管理器、同步器、七卷拾光收录

**门户收录（Portal Capture）**:
收录扩展面向维护者的产品名。名称跟门户源约定走，不跟某一部署的厅面字标走。
_Avoid_: 七卷拾光收录、导入器、MarksVault

**条目草稿（Entry Draft）**:
收录扩展面板里尚未写入门户源的一条待收录书签条目。面板可同时持有若干条草稿，一次确认一并写入。
_Avoid_: 导入项、待导入书签

**书签条目（Bookmark Entry）**:
由标题和 URL 定义的公开网站引用，必须至少有一个标签，可以附带描述。标签列表的第一项是主标签。书签目录内不允许出现重复标题或重复的标准化 URL。标题去除首尾空格并做 Unicode 标准化后按大小写敏感方式比较。描述可省略。
_Avoid_: 站点、链接、资源

**主标签（Primary Tag）**:
书签条目标签列表的第一项。无提问且无标签筛选时，货架按主标签分块。
_Avoid_: 分类、首分类、文件夹

**标签（Tag）**:
书签条目必须携带的至少一个主题名称，可再附带更多；空字符串不计入。系统从所有标签值自动汇总标签，不单独维护标签记录。标签名去除首尾空格并做 Unicode 标准化后仍按大小写敏感方式区分。同一条目内的标签去重。收录时优先从书签目录已有标签中选取；仅当已有标签无法表达该条目时才新增。
_Avoid_: 分类、文件夹、关键词、类目、多分类

**搜索厅（Search Hall）**:
门户的默认界面，用于提问并进入货架。
_Avoid_: 首页、落地页、检索页

**提问（Question）**:
访客在搜索厅或货架输入、用于从书签目录寻找目标的一段文字；它与标签约束互斥。
_Avoid_: 搜索词、关键词、查询语句

**货架（Shelf）**:
用卡片网格呈现书签条目的界面。没有提问词、也没有标签时展示全部目录；有提问或标签时只展示命中条目。提问与标签互斥：框提交是全站重搜，点标签进入该类并清空提问。结果顶列出本次命中相关标签，点按进入该类。无提问无标签时卡片仍按首个标签块的顺序排列，但不再插入分块标题。货架提问框与搜索厅同一套规则，避免为再问一遍先回厅。
_Avoid_: 导航站、二级页、搜索页、结果页（作为另一套布局）
