# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Vite 与原生 TypeScript；界面收成一组本项目组件。不使用 React、Vue 或 daisyUI。由 Vercel 在 GitHub `main` 分支更新后自动构建并发布到 `portal.jasper0507.me`。

## Users

首要使用者是维护者本人，通常在需要寻找资源站时打开搜索厅。访客可以浏览或搜索已发布内容，但不能修改书签。

## Product Purpose

集中呈现维护者收藏的网站，使维护者能够从搜索厅提问，并在货架上打开目标站。成功意味着目标书签能够被快速找到并打开。

## Positioning

这是围绕维护者个人收藏持续整理的公开书签目录，不是通用导航站或社区贡献平台。搜索厅是入口，货架是目录。

## Operating Context

维护者手工维护仓库中的单个 JSON 门户源（站点身份 + 书签目录）。改收藏或厅面文案都只改这份文件，推送到 GitHub `main` 后完成发布。页面上不提供添加面板或编辑模式。

迁移文件只在首次迁移浏览器书签时使用，不作为运行时输入或持续维护来源。

## Capabilities and Constraints

- 搜索厅是默认界面；厅架同一套提问规则。点建议或选亮后回车才打开站点或进入该类；未选亮回车与放大镜按提问词全站搜索，唯一命中也不打开。空回车进入全部货架；无匹配把提问带到货架空态。提问词在名称、描述、标签上模糊匹配，不搜 URL。点任何标签进入该类并清空提问，与提问词互斥。
- 书签条目由标题和 URL 构成，必须至少有一个标签；描述可选；不允许重复标题或重复的标准化 URL。
- 标签从条目自动汇总，不单独维护标签记录。搜索厅七词按引用次数最多取七个。
- 产品不设置账号、数据库或写入后端。

## Brand Commitments

克制纸面遵循 Archive Paper：浅档案纸、两字碑、金尘、一根提问线。第一版只做克制，并提供夜间纸（同一气质的黑夜纸）。大胆主题以后再做。Logo 回到搜索厅；夜间开关在页脚。

## Evidence on Hand

仓库现有 `CONTEXT.md` 与 `docs/adr/` 记录了领域术语、发布流程和前端技术栈。当前没有现成肖像 Logo、品牌图像、案例、评价或其他可用于公开背书的素材，未来设计不得虚构。

## Product Principles

- 优先缩短从产生需求到找到目标资源站的路径。
- 门户源始终是书签目录与厅面文案的唯一权威来源。
- 保持单维护者工作流简单、透明且可审查。
- 搜索厅保持一屏题签；货架才出现卡片。
