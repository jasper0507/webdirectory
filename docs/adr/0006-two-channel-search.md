# 搜索分提问词与标签约束两路，不做查询语言

个人目录规模小，Karakeep / Linkwarden 那种 `#tag`、`or`、括号会把搜索厅变成命令行。提问词按空白切分、AND、在标题 / URL / 描述 / 标签名里做子串；点七词、点列表标签、点结果页标签则是精确标签约束，多个约束之间也是 AND。排序为标题整句、标题前缀、标签全名等于提问词、其余子串。不引入拼音、模糊目录匹配或页面全文索引。
