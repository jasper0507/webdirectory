# 收录扩展放在独立仓库

收录扩展有自己的清单、打包、凭证和浏览器 API 生命周期，和这份静态门户不是同一次发布。把它塞进门户仓库，会把扩展工具链带进只该关心厅面与门户源的树。因此扩展单独成仓，公开仓库为 `jasper0507/webdirectory-capture`，产品名门户收录：fork 门户源码不会自带扩展；维护者安装一次后，在选项里指向自己的 owner/repo。扩展源码不进本仓；两仓除门户源约定外，共同依赖从本仓规则抽出的共享包。

不把扩展放进本仓库。不上架商店。
