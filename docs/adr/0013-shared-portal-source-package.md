---
status: proposed
---

# 门户源规则抽成共享包

收录扩展与门户必须用同一套标题/URL/标签规范化和去重，否则扩展放行的条目会在解析时被丢掉，或目录里已有的条目会被再写一次。扩展又住在独立仓库，复制一份函数之后一定会漂。因此从本仓抽出极小的共享模块，门户直接引用源码；收录扩展在构建期用 GitHub 依赖钉到本仓的 tag（或 commit），不发 npm。权威仍在本仓的门户源规则，不在扩展仓另写一套。

不采用扩展仓内自写近似规则，也不为这个模块做 registry 发版。模块只放门户源的读写约定（规范化、校验、去重、序列化），不放搜索、路由或厅架 UI。
